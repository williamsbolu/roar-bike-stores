import styles from './ButtonFormSpinner.module.css';

const ButtonFormSpinner = () => {
    return <div className={`${styles.sp} ${styles['sp-circle']}`}></div>;
};

export default ButtonFormSpinner;
