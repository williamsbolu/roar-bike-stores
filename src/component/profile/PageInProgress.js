const PageInProgress = () => {
    return (
        <div className="centered">
            <p>Page not available currently. check back later!</p>
        </div>
    );
};

export default PageInProgress;
